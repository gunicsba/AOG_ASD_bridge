name: Build AOG-ASD

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pyinstaller pyserial

    - name: Build AOG-ASD
      run: |
        $ICON_FLAG = ""
        if (Test-Path "icon.ico") {
          $ICON_FLAG = "--icon=icon.ico"
          Write-Host "Using icon: icon.ico"
        } else {
          Write-Host "WARNING: icon.ico not found!"
        }
        pyinstaller --onefile --console --name "AOG-ASD" $ICON_FLAG "AOG_ASD_bridge.py"
        pyinstaller --onefile --console --name "AOG-ASD-Sniffer" $ICON_FLAG "asd_sniffer.py"
        pyinstaller --onefile --console --name "AOG-ASD-Probe" $ICON_FLAG "asd_probe.py"

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: AOG-ASD
        path: |
          dist/AOG-ASD.exe
          dist/AOG-ASD-Sniffer.exe
          dist/AOG-ASD-Probe.exe

    - name: Create Release
      uses: softprops/action-gh-release@v2
      with:
        files: |
          dist/AOG-ASD.exe
          dist/AOG-ASD-Sniffer.exe
          dist/AOG-ASD-Probe.exe
        generate_release_notes: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
